# Handoff — журнал передачи проекта Author

Этот файл — память проекта между сессиями. Правила его ведения — в `CLAUDE.md`.

## В РАБОТЕ

*(Пусто. Активной незавершённой задачи нет.)*

<!--
  Когда начинается задача разработки, заглушка выше заменяется блоком:
  - **Задача:** …
  - **План:** …
  - **Файлы:** …
  - **Критерий готовности:** …
  По завершении блок снова становится заглушкой, а итог уходит в журнал.
  Раздел читает SessionStart hook: любой текст здесь, кроме заглушки *(…)*,
  считается незавершённой работой.
-->

## Замечания автора — бэклог (пачка №4, 2026-08-01, тест «Тёплого кабинета»)

**Сделано в этой пачке (визуальное):**
- [x] Бейдж «СЕКРЕТАРЬ» — жирный, тёмный (читаемость).
- [x] Разбелённая бирюза (--accent-soft) на карточках Кабинета, плашках Версии
      («в следующих версиях») и дверях лендинга (Стол/Кабинет).
- [x] Фон приложения посветлел/менее насыщенный (--page #ece4cf) — искать
      оттенок дальше, если всё ещё раздражает.

**Осталось / обсуждаем:**
- [x] Голос секретаря с характером — пулы реплик + случайный выбор (готово).
- [x] «Проход»: слово не возвращаем; добавлена статья «Круг работы» в Азбуку.
- [x] Азбука-дубль: карточку из Кабинета убрали, осталась в панели.
- [x] Сводка: остаётся в 1.0 (это про одну тетрадь, не дубль Сверки голоса);
      поставлена защита от размножения черновиков; глоссы разведены в Азбуке.
- [x] Палитра приведена к референсу (River/Hilton/Nessi/Calvados), затем
      докручена на энергию: почти-белые поверхности + живее петроль + ярче
      янтарь (фон больше не «раздражает»).
- [x] «Дельта-таблица: как растёт ваш голос» — переименовано (по согласию).
- [x] Правило в CLAUDE.md: ничего не изымать/переименовывать без согласия автора.
- [x] **Компас** — по согласию автора убран из UI (→ «наставник»): NewPassForm,
      ошибка actions, депеша в prompts. Сущность Compass в коде цела.

## Замечания автора — бэклог (пачка №3, 2026-08-01)

Собрано с тест-прохода. Ветка обсуждения — часть решаем сразу, часть большим
разговором (дизайн, путь автора).

**Быстрые правки / баги:**
- [x] Навигация: «Азбука» и «Версия 1.0» вынесены в верхнюю панель (после «Кабинет»).
- [x] Кнопки-отклики (Сводка/Обновить): теперь держатся до крестика, на
      акцентной плашке (Batch B).
- [ ] Экспорт: «Word» скачивает .rtf (сбивает), TXT/Word — сразу в загрузки, PDF —
      открывает страницу. → унифицировать процесс + «починить Word» (решить:
      настоящий .docx с разовой установкой ИЛИ оставить .rtf).

**Сопровождение пути (подсказки секретаря):**
- [x] Возврат к давнему тексту: приветствие-ориентир (Batch B).
- [x] Сообщение «Итерация не закрыта…» → «Сейчас ваш ход: …» (Batch A).
- [x] Подсказки секретаря — заметная плашка с крестиком (Batch B).

**Большой разговор:**
- [ ] Дизайн (архитектурный + визуальный): всё в одной гамме, «уныло». Роли:
      Пётр Фролов × Ниман × Джони Айв (Apple). Заголовочный комплекс (морской)
      — нравится, оставить. Нужны варианты: фон/цвет вспомогательных кнопок и т.д.
- [ ] Путь автора / юзабилити: иерархия возможностей, онбординг новичка,
      ненавязчивая система подсказок, ведущая по работе с текстом. Цель — просто,
      но вовлекающе; автор чувствует, что растёт. Формат: я задаю вопросы, автор
      отвечает, вместе строим систему.

## Журнал выполненных задач

- **2026-08-01** — Убран «компас» из интерфейса (по согласию автора) → всюду
  «наставник»: NewPassForm («выберите наставника», «семь осей наставника»,
  «родной жанр наставника»), ошибка в actions.ts, депеша в prompts.ts («разбор
  по ориентирам наставника», «файл наставника»). Внутренняя сущность Compass
  (lib/compasses.ts, *-COMPASS.md, compassId и т.п.) не тронута — на слове UI
  ничего не держалось. Словарь помечен. 86 тестов, сборка зелёные.

- **2026-08-01** — Палитра-энергия + Дельта-таблица + правило. (1) Палитра
  приведена к референсу автора, затем докручена: почти-белые поверхности
  (--surface/--card) на светлой грейж — акценты «звенят»; петроль живее
  (--accent #16776c), янтарь ярче (--amber #cba23c). Диагноз «унылого»: не
  хватало белого и насыщенности (всё было в среднем грейже). (2) «Как двигается
  голос» → «Дельта-таблица: как растёт ваш голос» (по согласию автора; заодно
  вернули слово «дельта» в UI осознанно). (3) В CLAUDE.md — правило: ничего не
  изымать/переименовывать без согласия автора, сначала обсуждать. Прод-сборка,
  живые скрины — зелёные. NB: «компас» в UI — на обсуждении (не тронут).

- **2026-08-01** — Пачка №4 (по тесту): (1) Защита Сводки — пока есть
  незавершённый черновик сводки, новый не создаётся (`createDigest`), чтобы не
  плодились пустые «Сводка · черновик». (2) Убрана карточка «Азбука мастерской»
  из Кабинета (осталась в верхней панели): Кабинет = рабочие комнаты, Азбука =
  справка. (3) В Азбуку добавлена статья «Круг работы» (линза→депеша→разбор→
  правка→версия — ключевой цикл, без возврата слова «проход»); глоссы «Сводка»
  (про одну тетрадь) и «Сверка голоса» (про весь стиль) разведены — они не
  дубль. (4) Голос секретаря с характером: `lib/secretaryLines.ts` — пулы
  RETURN_OPENERS / SVERKA_LINES / RHYTHM_LINES (по 6–7 реплик) + `pickLine`
  (стабильно-случайный выбор), вписаны в приветствие тетради и напоминания
  Стола. Тон «в очках, себе на уме»: оберегает, но подначивает. В ВЕРСИИ.md —
  v2: генерация реплик на лету со своим API-ключом. 86 тестов (было 82),
  прод-сборка, живые скрины — зелёные.

- **2026-08-01** — Дизайн: тема «Тёплый кабинет» (В1, выбор автора после
  сравнения трёх макетов: Фролов×Ниман×Айв). Лечим «плоскую бежевую гамму» —
  добавлена глубина/слои: новый токен `--page` (глубокий фон), `--card` светлее,
  `--shadow`; body → `--page`; мягкие тени на карточках (notebook-card,
  fragment-pane, pass-card, study-card, lens-block, version-milestone,
  more-menu-body, активные mentor-card). Янтарный акцент из В3 (`--amber`) —
  на бейдже плашки секретаря. lens-button фон → --surface (светлее панели).
  Сохранено: шоколадная нав-панель с карамель→бирюза (hover/active), морской
  заголовочный комплекс, чёрные названия линз, счётчик «слов·знаков». Прод-
  сборка + живые скрины (Стол/тетрадь/Кабинет) — зелёные. Макеты 3 вариантов —
  в scratchpad (не в репо). Возможные доводки на будущее: заливка-бирюза у
  главных кнопок, стойкая бирюза у активной нав-кнопки, дозировка янтаря.
  (Лимита символов в тетради нет — поэтому «остатка знаков» не существует.)

- **2026-08-01** — Путь автора, Batch C (онбординг новичка). Новая страница
  `/start` «С чего начать» — квик-старт за минуту: главное (ИИ не пишет за
  вас), пять шагов первого круга (тетрадь → линза → разбор → правка → версия),
  «где что лежит» (Стол/Кабинет/Азбука), крупная кнопка «Открыть Стол и завести
  первую тетрадь →». Ссылки: лендинг («Впервые здесь? С чего начать →»), пустой
  Стол, подвал на всех страницах. 82 теста, прод-сборка, живой скрин — зелёные.
  ПУТЬ АВТОРА (A+B+C) ПРОЙДЕН. Дальше: экспорт (настоящие docx/pdf) и большой
  дизайн (Фролов × Ниман × Айв).

- **2026-08-01** — Путь автора, Batch B (подсказки секретаря). Новый компонент
  `SecretaryNote` — заметная плашка «Секретарь» (акцентная рамка/бейдж) с
  крестиком; закрытие запоминается в localStorage по id (сменится ситуация →
  сменится id → вернётся). Напоминания на Столе (аудит/ритм) переведены на неё.
  В тетради — приветствие при возврате: «Вы не были здесь N дней. В прошлый раз
  вы отметили: „…". Сейчас ваш ход: …» (через `daysSince` + последняя версия +
  закон итерации; порог 3 дня). Кнопки-отклики (`ToolbarActionButton`) больше
  не убегают за 3–4 сек — держатся, пока не уберёшь крестиком, на акцентной
  плашке. 82 теста (было 80), прод-сборка, живые скрины (приветствие 27 дней +
  Стол) — зелёные. Дальше: Batch C («С чего начать»), затем экспорт и дизайн.

- **2026-08-01** — Путь автора, Batch A (чистка тетради). По итогам интервью с
  автором: (1) слово «проход» убрано из интерфейса → справа в тетради теперь
  «Разборы» (заголовок, пустая заметка, счётчики на Столе/Картотеке/Полке,
  ошибки удаления, сообщение сводки, Азбука, Словарь; во внутреннем коде тип
  Pass и «проход» в комментариях остались). (2) Сообщения закона итерации
  переписаны с «нельзя» на «сейчас ваш ход: …» (что нажать дальше).
  (3) Иерархия тетради: вспомогательное (картотека, полка, экспорт,
  переименовать, удалить) убрано под нативный `<details>` «⋯ Ещё…»; на виду —
  текст и линзы. «Сводка секретаря» переехала в aside к разборам с подписью
  «свести все разборы в общий итог». 80 тестов, прод-сборка, живые скрины
  (чистая тетрадь + раскрытое «Ещё…») — зелёные. Дальше по пути: Batch B
  (подсказки-плашки + приветствие при возврате), Batch C («С чего начать»),
  затем экспорт (настоящие docx/pdf) и большой дизайн.
  Решения по модели/языку записаны в бэклоге выше.

- **2026-07-31** — «Азбука мастерской» — страница-глоссарий в Кабинете
  (`/study/azbuka`): тёплые пояснения слов мастерской для автора, сгруппированы
  (Территории, Круг работы «Прописи», Разделы Кабинета, Ритуал разбора). Слова
  — из `docs/СЛОВАРЬ.md`, но человеческим языком. Добавлена карточка на индексе
  Кабинета; заодно поправлен устаревший текст карточки «Голос» (дельты уехали в
  Наставники) — теперь про сверку и механики, а замеры перенесены на карточку
  наставников. Стили `.glossary`. 80 тестов, прод-сборка, живой скрин зелёные.
  ЭТО БЫЛА ПОСЛЕДНЯЯ ЗАДАЧА ИЗ ОТЛОЖЕННОГО СПИСКА (пачки правок №2 + идей).

- **2026-07-31** — Экспорт тетради: Word · PDF · TXT. На странице тетради блок
  «Забрать текст» (реачабелен из Картотеки/Полки — они ведут на тетрадь).
  Экспортируется чистовик (заголовок + финальная версия). Без тяжёлых
  зависимостей, чтобы у автора `git pull` + `npm run dev` работал без
  `npm install`: Word — RTF (Word открывает нативно, кириллица через
  \u-эскейпы, `ansicpg1251`), TXT — простой UTF-8, PDF — печать браузера
  («Сохранить как PDF»). Новое: `lib/export.ts` (buildManuscript, toPlainText,
  toRtf, safeFileName), роут `app/desk/[id]/export/route.ts` (GET
  ?format=rtf|txt, Content-Disposition с filename* для кириллицы), печатная
  страница `app/desk/[id]/print/page.tsx` + `components/PrintButton.tsx`,
  print-CSS прячет шапку/подвал/панель. 80 тестов (было 72, +8), прод-сборка,
  живая проверка (RTF-заголовки, кириллица в \u, печатный вид) — зелёные.

- **2026-07-31** — Заведён журнал версий `docs/ВЕРСИИ.md` — живой рабочий
  чертёж отличий 1.0 ↔ 2.0 ↔ 3.0: таблица-обзор + подробные заметки «как это
  будет работать» (личный API-ключ, свои наставники, полный аудит корпуса,
  доступ по ссылке) с зацепками в коде и открытыми вопросами. В отличие от
  витрины `/version` — это чертёж для сборки следующих версий. В `CLAUDE.md`
  добавлено правило: решение «упрощаем/откладываем до v2» вписывается в
  `docs/ВЕРСИИ.md` автоматически, без просьбы. Только docs — код не менялся.

- **2026-07-31** — Паспорт версии + «Сверка голоса» (упрощённый аудит для 1.0).
  (1) Аудит корпуса упрощён до **«Сверки голоса»** — лёгкого зеркала: депеша
  `buildVoiceCheckPrompt` просит назвать 2–3 повторяющиеся черты правок (что
  уверенно / что качается), одна секция `[СЕКЦИЯ: сверка]`. `startAudit` →
  `startVoiceCheck`; `submitPassResponse` для типа audit больше НЕ пишет
  md-файл (машинерия v2), результат живёт на проходе. Дата последней сверки —
  с завершённого прохода (`lastVoiceCheckDate` + `laterDate`, объединяются с
  легаси md-датой), напоминание на Столе гаснет само. Голос перестроен:
  «Сверка голоса» + «История сверок» (завершённые проходы) + «Ядро голоса»
  (портрет, если есть); md-архив «Аудиты» убран. passMeta audit → «Сверка
  голоса». Полный «Аудит корпуса» (авто-ядро, авто-замеры, история) —
  приберечь под v2 (функции buildAuditPrompt/buildAuditMarkdown/writeAuditFile
  оставлены в коде и тестах для будущего). (2) Новая страница **«Паспорт
  версии»** `/version`: что умеет 1.0, границы, роадмап (v2 — свои наставники,
  личный API-ключ, полный аудит; v3 — доступ по ссылке); ссылки с лендинга и
  из подвала. Словарь обновлён. 72 теста (было 68, +4), прод-сборка, живые
  скриншоты паспорта и Голоса проверены.

- **2026-07-31** — Кнопки-отклики. «Сводка секретаря» и «Внести/Обновить в
  картотеке» на странице тетради теперь показывают, что нажатие сработало:
  пока идёт работа — «Собираю сводку…» / «Вношу…» / «Обновляю…» на самой
  кнопке (disabled), после — короткий отклик-галочка «✓ …» рядом (гаснет через
  4 сек). Раньше «Обновить в картотеке» не давал видимого эффекта вовсе.
  Реализация: `createDigest` и `commitToCorpus` возвращают `ToolbarResult
  {ok,message}`; новый клиентский компонент `ToolbarActionButton`
  (useActionState) с pending и flash. CSS `.toolbar-flash` (морской для
  успеха, карамель для отказа). 68 тестов, прод-сборка, живой клик-тест
  (flash «✓ Обновлено в картотеке.») проверены.

- **2026-07-31** — Разнесли «Голос» и «Дельты». Дельта-таблицы наставников
  (замеры голоса по осям) переехали с «Голоса» на страницу «Наставники» —
  раздел «Как двигается голос» под активными карточками, свёрнутый по каждому
  наставнику (`readDeltaTables()` + сортировка «задействованные первыми»). На
  «Голосе» дельт больше нет, md-имена файлов спрятаны: «Ядро голоса» вместо
  AUTHOR-VOICE-CORE.md, «Аудит от D месяца YYYY» / «Аудит за месяц YYYY»
  вместо AUDIT-YYYY-MM[-DD] (хелпер `auditLabel`). Ссылка-переход с «Голоса»
  на «Наставники». Словарь обновлён. 68 тестов, прод-сборка, живые
  скриншоты обеих страниц проверены.

- **2026-07-31** — Чистка словаря изысканий (слово «наставник» неуместно):
  изыскание — это научная справка, а не проход по наставнику. Заведены
  отдельные статусы изыскания (`PASS_STATUS_LABELS_INQUIRY`): «запрос
  отправлен» вместо «у наставника», «справка получена» вместо «разбор
  получен»; PassCard выбирает их по `isInquiry`. Кнопка вставки ответа для
  изыскания — «Вставить ответ» (без «наставника»). На страницу «Изыскания»
  добавлен вводный абзац — что это и чем отличается от разбора. Словарь
  дополнен разделом «Статусы изыскания». 68 тестов, прод-сборка зелёные.
  (Старые путаные данные изысканий — в локальной data/ автора, кодом не
  правятся.)

- **2026-07-22** — Пачка правок по тесту №2 (тексты + заголовки + Наставники):
  лендинг — рубрика «Мастерская автора текста», заголовок и разделы теперь
  глубокий морской, сокращённый мотто, новые тексты «Как это работает» и
  «Пути автора», тексты дверей — по формулировкам автора; заголовочный
  комплекс (h1/h2) сделан крупным и морским (var(--accent)) во всём
  приложении; вводный текст Стола обновлён; страница «Наставники»
  перестроена — 3 активных крупным первым рядом, неактивные компактным
  подвалом под «Наставники: встретиться в следующих версиях» (метка с
  карточек убрана). Словарь обновлён. 68 тестов, прод-сборка проверена
  вживую. ОТЛОЖЕНО (ответы/план даны автору): экспорт Word/PDF, «Азбука
  мастерской», логика изысканий, разнесение Аудита и Дельт (Голос/Наставники).

- **2026-07-22** — Безопасное обновление (часть в коде): папка `data/`
  (тетради/версии/проходы) вынесена из-под git — добавлена в `.gitignore`,
  `git rm --cached data/*.json` (файлы остались на диске и в истории), папка
  сохранена через `data/.gitkeep`; пометка в CLAUDE.md «данные автора вне
  git». Теперь обновление кода не затирает работу автора; фолбэк на пустую
  data/ уже был в storage.ts. 68 тестов зелёные. Дальше — провести автора по
  разовой настройке git (install Git → clone → положить свою data → git pull).

- **2026-07-22** — Лендинг «Стол и Кабинет» (часть Шага 2): по адресу «/»
  теперь приветственный первый экран — что это, «как это работает» (ИИ даёт
  разбор, пишешь ты), «пути автора» (базовый круг / одна линза / свой
  маршрут) и две двери Стол/Кабинет. Бренд в шапке ведёт на лендинг.
  app/page.tsx (был redirect на /desk), layout, стили. 68 тестов, прод-сборка
  проверена вживую. Дальше в Шаге 2: рамка-статус вокруг окна + легенда
  этапов, подсветка «что нажать сейчас».

- **2026-07-22** — Шаг 1 правок по тесту автора (баги и чистота экрана):
  (1) завершённые проходы свёрнуты по умолчанию — «колбаса» убрана;
  (2) промпт спрятан от пользователя — видимого блока «Депеша (промпт)» нет,
  осталась только кнопка «Скопировать» (текст промпта в UI не показывается);
  (3) кнопка изысканий убрана со Стола (место — в Кабинете);
  (4) раскладка окна текста: редактируемое окно текущей версии — наверх
  героем со счётчиком, «Было→стало» и «Путь фрагмента» — свёрнуты под ним;
  (5) плейсхолдер ответа нейтральный «ответ от вашего ИИ-сервиса»;
  (6) сырые имена md-файлов убраны из тетради и Картотеки, добавлен
  «← Кабинет» на подстраницах Кабинета. 68 тестов, прод-сборка проверена
  вживую в Chromium. Дальше по плану теста: Шаг 2 (лендинг «Стол и Кабинет»,
  рамка-статус + легенда этапов, подсветка «что нажать»), затем Шаги 3–5.

- **2026-07-20** — Свели дизайн-прототип и рабочее приложение + завели
  ИСТОЧНИКИ ПРАВДЫ, чтобы больше не расходилось. (1) Найдено: дизайн и словарь
  жили в отдельной невлитой ветке claude/localhost-access-issue-awg3aq
  (docs/, прототип stol-proba-4.html, GitHub Pages) — рабочий Next-app их не
  наследовал. (2) Дизайн-токены прототипа перенесены в app/globals.css: беж
  #f1e8d2, чернила #2b2416, бирюза #0e6f68, карамель #b98a2e, шрифты
  Georgia/Times/Courier, «чердак»-полоса, ховеры навигации. (3) Заведён
  единый docs/СЛОВАРЬ.md + правило в CLAUDE.md «текст — из словаря, цвет — из
  токенов». (4) Лексика по коду: бренд «Стол и Кабинет», вводный текст,
  «диагноз»→«Разбор» (контракт + читатели + legacy-фолбэк для старых записей),
  линзы Не высушить/Сверить/Усилить, статусы, кнопки (Депеша наставнику,
  Положить в тетрадь, Письмо мастеру), Голос/Наставники. 68 тестов зелёные,
  прод-сборка проверена вживую в Chromium. NB: незакрашенный вид страницы на
  первую долю секунды в `npm run dev` — артефакт on-demand компиляции CSS,
  не баг; обновить страницу или смотреть на `npm run build && npm start`.

- **2026-07-20** — Переименование и удаление тетради (последний пункт очереди
  аудита UX): на странице тетради инлайн-переименование (пустое имя
  отклоняется) и удаление с двухшаговым подтверждением — тетрадь уходит с
  версиями и проходами, редирект на Стол (файл кейса в картотеке не трогаем).
  `cleanTitle`/`removeNotebook` в `lib/notebook.ts`, `renameNotebook`/
  `deleteNotebook` в actions, `NotebookControls.tsx`. 68 тестов зелёные, E2E
  в Chromium пройден (переименование → новое имя на Столе; удаление →
  редирект, 404 при прямом заходе). ОЧЕРЕДЬ АУДИТА UX ЗАКРЫТА — все 14
  пунктов внедрены.

- **2026-07-20** — Констатация ритма (ТЗ §5.5): на Столе тихая строка секретаря
  «за последние 30 дней — N зафиксированных правок» — фактом, не похвалой.
  Окно 30 дней, порог 5; правкой считается версия, кроме первой в тетради.
  При висящем напоминании об аудите ритм не показываем (у секретаря один
  голос). `workRhythm` в `lib/rituals.ts`. 65 тестов зелёные, вживую
  проверены оба случая (ритм виден; аудит его перекрывает). Очередь дальше —
  последнее: переименование/удаление тетради.

- **2026-07-20** — Карта наставников (`/study/mentors`): все 13 компасов на
  карте, но задействованные в этой версии (Чехов, Довлатов, Соркин — боевые
  файлы) яркие и «заполняются» точками по числу завершённых проходов (счётчик
  + дата последнего); остальные 10 приглушены (grayscale + прозрачность) с
  пометкой «в следующих версиях» — витрина будущих версий. Селектор компаса в
  линзе теперь предлагает только активных (`ACTIVE_COMPASSES`), чтобы автор не
  звал наставника с файлом-каркасом. `lib/mentors.ts` (mentorEngagement) +
  флаги в `lib/compasses.ts` + карточка на /study. 62 теста зелёные, E2E в
  Chromium пройден. Очередь дальше: констатация ритма, переименование/удаление
  тетради.

- **2026-07-20** — Метка «ждёт правки» на Столе: тетрадь с полученным, но не
  закрытым версией диагнозом несёт тихую метку «диагноз получен — ждёт
  правки» (через findPassToClose из закона итерации, без новой логики).
  Проверено вживую: метка появляется при незакрытом диагнозе и исчезает после
  фиксации версии. Очередь дальше: карта наставников, констатация ритма,
  переименование/удаление тетради.

- **2026-07-20** — «Путь фрагмента» — главный пэйофф итераций (ТЗ §5.2/§13.5):
  в тетради с 2+ версиями раскрывается блок с сводкой пути («версий: N ·
  слов: X → Y»), лентой вех (дата, объём, дельта слов, пометки автора) и
  диффом первой ↔ последней версии. Заодно закрыт пункт «даты у чипов
  версий» (тултипы). `lib/path.ts` (pathSummary, pathMilestones,
  formatDelta) + блок в FragmentPane. 58 тестов зелёные, E2E в Chromium
  пройден. Очередь аудита UX дальше: метка «ждёт правки» на Столе, карта
  наставников, констатация ритма, переименование/удаление тетради.

- **2026-07-20** — PR #2 влит в `main` кнопкой слияния (все 5 фич на главной
  ветке). Заведена процедура «main всегда актуален» (правило в `CLAUDE.md`):
  по завершении задачи `main` автоматически подтягивается к рабочей ветке через
  PR + слияние, без вопроса пользователю; раз в 3 дня триггер-подстраховка
  проверяет отставание. `main` — не источник публикации, риска нет.

- **2026-07-19** — Заведён алгоритм сохранения контекста: добавлены `CLAUDE.md`
  (правила проекта, три слоя защиты) и `handoff.md` (этот журнал). Код проекта
  не менялся.
- **2026-07-19** — Счётчик объёма (п. В-6 аудита UX): под окном текста тихая
  строка «слов: N · знаков: M» по текущему черновику (`lib/textStats.ts`,
  знаки — с пробелами). 52 теста зелёные. Очередь аудита UX дальше: «путь
  фрагмента» (дифф первой ↔ последней версии), метка «ждёт правки» на Столе,
  карта наставников, констатация ритма, ссылка на claude.ai у депеши,
  переименование/удаление тетради, даты у чипов версий.
- **2026-07-19** — Блок А аудита UX, три тупика закрыты. (1) Удаление
  незавершённого прохода: `removePass` + `deletePass` + тихая кнопка с
  двухшаговым подтверждением; ошибочный черновик больше не блокирует тетрадь,
  тетради-призраки подчищаются. (2) Автосейв черновика: правка в окне текста
  живёт в localStorage, переживает F5 и уход со страницы, восстанавливается с
  тихой строкой и «отбросить»; предупреждение beforeunload. (3) Аудит из
  приложения: тип прохода `audit`, кнопка «Провести аудит» на «Портрете
  голоса», депеша секретаря из пар «было ↔ стало» (`lib/audit.ts`,
  `buildAuditPrompt`), ответ пишется в `learning/audits/AUDIT-дата.md` и сам
  гасит напоминание. Попутно E2E нашёл и починил баг `readLastAuditDate`:
  выбор файла по имени брал старый месячный аудит вместо нового дневного —
  теперь максимум по дате. 49 тестов зелёные, полный E2E в Chromium пройден.
- **2026-07-19** — Флоу «Новая тетрадь» на Столе: автор может завести тетрадь с
  нуля (название + первый текст → тетрадь с первой версией, редирект на её
  страницу). Добавлены `lib/notebook.ts` (чистый билдер `buildNewNotebook`),
  `createNotebook` в `app/desk/actions.ts`, `components/NewNotebookForm.tsx`,
  кнопка на `app/desk/page.tsx`, стили и `tests/notebook-create.test.ts`.
  Проверено вживую: тетрадь появляется на Столе, её страница готова к проходу.
  34 теста зелёные, typecheck и build чистые.
- **2026-07-19** — Слой 3 стал автоматическим: добавлен SessionStart hook
  (`.claude/hooks/session-start.sh` + `.claude/settings.json`). При старте
  каждой сессии он читает раздел «В РАБОТЕ» и git-состояние и, если есть
  незавершённое, сам просит показать отчёт восстановления — копировать промпт
  вручную больше не нужно.

## Промпт восстановления

Обычно отчёт восстановления запускает SessionStart hook автоматически. Этот
промпт — запасной вариант: если хук ещё не в default-ветке или не сработал,
скопируй и пришли в новый чат текст ниже:

---

Сессия по проекту Author оборвалась. Прежде чем что-либо делать:

1. Прочитай `CLAUDE.md` и `handoff.md` в репозитории
   `methodspoint-afk/Author`.
2. Посмотри раздел «В РАБОТЕ» в `handoff.md` и последние коммиты во всех
   ветках (включая `claude/author-wip`, если она есть).
3. Покажи мне отчёт: **что сделано / что потеряно / план дальнейших действий**.
4. НЕ трогай ветку `main` и НЕ принимай никаких решений, пока я не скажу «ок».

---
