node_modules/
dist/
.next/
*.tsbuildinfo

# Данные автора (тетради, версии, проходы) — НЕ код. Живут только на машине
# автора; обновление кода их не трогает. Папка сохраняется через data/.gitkeep.
/data/*.json
/data/.*.tmp
